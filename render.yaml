services:
  - type: web
    name: wifi-call
    runtime: node
    plan: free
    rootDir: wifi-call
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 24
